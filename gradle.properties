# ForgeGradle 构建参数
org.gradle.jvmargs=-Xmx3G -Dfile.encoding=UTF-8
org.gradle.parallel=true

# ------------------------------------------------------------------
# Curios API 版本
# 仅在 build.gradle 中启用 Curios 依赖（取消注释）时才被读取。
# 1.20.1 Forge 最新稳定版：5.14.1+1.20.1（官方 Maven 确认）
# ------------------------------------------------------------------
curios_version=5.14.1+1.20.1
